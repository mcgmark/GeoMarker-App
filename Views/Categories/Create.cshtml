@model GeoMarker.Models.Category

@{
    ViewData["Title"] = "Create Category : GeoMarker";
    ViewData["Heading"] = "Create Category";
    ViewData["Content"] = "Start a new marker category.";
}

<div class="p-5 pb-5 bg-white mb-3 shadow-sm">
    <div>
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <span class="bg-primary p-2 rounded">
                <i class="fa-solid fa-check text-white"></i><input type="submit" value="Create" class="btn text-white" />
            </span>
                <div class="border-primary"><a class="p-2 display-7 text-white bg-primary rounded text-decoration-none" asp-action="Index"><i class="fa-regular fa-rectangle-xmark"></i>Cancel</a></div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
